# Listen_India
sign language recognisation  with voice 
# This project is ready and trained 
# you can run this any database
#Just install CNN and keras libraries.
#you can run the project either in IDLE or Microsoft Visual Code
#1. Run the set_hand.py file to set capture the image and set thresh for hand signs
the database has been stored and images are trained with a output file with extention ".H5".
2.run the file "finalwithvoice.py"
Your Project is ready.
